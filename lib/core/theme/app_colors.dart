import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary colors
  static const Color primary = Color(0xFF0D6EFD);
  static const Color primaryLight = Color(0xFF1B8AFF);

  // Accent colors
  static const Color accentOrange = Color(0xFFFF7029);
  static const Color accentTeal = Color(0xFF24BAEC);

  // Background colors
  static const Color splashBackground = Color.fromARGB(255, 11, 60, 76);
  static const Color white = Color(0xFFFFFFFF);
  static const Color background = Color(0xFFF5F5F5);
  static const Color inputBackground = Color(0xFFF7F7F9);

  // Text colors
  static const Color textPrimary = Color(0xFF1B1E28);
  static const Color textSecondary = Color(0xFF7D848D);
  static const Color textLight = Color(0xFFCACACA);

  // Other colors
  static const Color divider = Color(0xFFE5E5E5);
  static const Color shadow = Color(0x1A000000);
}
